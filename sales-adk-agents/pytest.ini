[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = --import-mode=importlib -m "not integration"
markers =
    integration: mark test as requiring a live LLM/ADK (deselected in CI)
